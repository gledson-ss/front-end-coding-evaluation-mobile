export interface inputFieldProps {
  name: string;
  email: string;
  date: string;
  cep: string;
}

export interface inputProps {
  field: string;
  placeholderValue: string;
  value: string;
  context: string;
}
